#!/usr/bin/python
class Executor:

    @staticmethod
    def run(inputs):
        MF = "F-M"
        MM = "M-M"
        FF = "F-F"
        for rallonge in inputs:
            l = rallonge.split()
        return

Executor.run(['M-F 187', 'F-F 215', 'M-M 128', 'M-M 128'])